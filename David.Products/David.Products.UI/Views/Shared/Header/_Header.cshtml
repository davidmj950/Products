@using David.Products.UI.BusinessLayer.Helpers

@{
    Layout = null;
}

@if (string.IsNullOrEmpty(SessionHelper.Id))
{
    ViewBag.ScriptUp = "ShowError('Debe ingresar sus credenciales');";
    Response.Redirect("~/Account/Index", false);
    //Response.RedirectToRoute("~/Account/Login", new { ViewBag.StartUpScript });
}
else
{
    <!-- Image and text -->
    <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-primary">
        <a class="navbar-brand" href="#"><span>Finance</span></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor02">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item active">
                    <a class="nav-link" href="@Url.Action("Index", "Home")">Home<span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="@Url.Action("Index", "User")">Users</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">Pricing</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">About</a>
                </li>
            </ul>
        </div>
    </nav>
}